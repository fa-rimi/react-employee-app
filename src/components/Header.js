import "../App.css";

function Header() {
    return (

        <div className="header-section">
            <h1>Header</h1>
            {console.log("Header is live!")}
        </div>

    )
}

export default Header;